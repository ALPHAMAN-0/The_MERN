# Docs folder

Place repository images and documentation assets in this folder.

To show the architecture diagram in the top-level `README.md`, add the image as `architecture.png` here:

```bash
# from the repo root
cp /path/to/diagram.png docs/architecture.png
```
